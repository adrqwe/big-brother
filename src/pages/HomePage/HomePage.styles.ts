import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  container: {
    position: "fixed",
    top: 0,
    left: 0,
    width: "100vw",
    height: "100vh",
  },
  gridItem: {
    backgroundColor: "red",
  },
});
